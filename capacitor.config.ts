import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'scribble.scrable',
  appName: 'scribble',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
